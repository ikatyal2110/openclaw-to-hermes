# @praxis/bridge — placeholder

Slot reserved for the v0.3+ hybrid runtime: a process that exposes Hermes-compatible tool descriptors backed by live OpenClaw plugin invocations. Empty in v0.1.

See [`docs/architecture.md`](../../docs/architecture.md) for the eventual shape and the explicit reasons it's deferred out of MVP.
