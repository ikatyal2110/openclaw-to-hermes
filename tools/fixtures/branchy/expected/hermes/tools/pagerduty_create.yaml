name: pagerduty_create
description: Create a PagerDuty incident via the v2 events API.
runtime: http
spec: {}
inputs:
- name: token
  type: string
  required: true
- name: summary
  type: string
  required: true
outputs:
- name: dedup_key
  type: string
